{
  "name": "finsphere-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=finsphere-backend\" \"npm run dev --workspace=finsphere-frontend\"",
    "build": "npm run build --workspace=finsphere-backend && npm run build --workspace=finsphere-frontend",
    "lint": "npm run lint --workspace=finsphere-frontend && npm run lint --workspace=finsphere-backend",
    "typecheck": "npm run typecheck --workspace=finsphere-backend && npm run typecheck --workspace=finsphere-frontend",
    "test": "npm run test --workspace=finsphere-backend",
    "test:e2e": "npm run test:e2e --workspace=finsphere-frontend",
    "db:generate": "npm run db:generate --workspace=finsphere-backend",
    "db:migrate": "npm run db:migrate --workspace=finsphere-backend",
    "db:seed": "npm run db:seed --workspace=finsphere-backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2"
  }
}
